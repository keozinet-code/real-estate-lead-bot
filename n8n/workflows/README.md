# n8n workflows

Store sanitized exports here; never commit credentials. Use `NN-purpose.version.json`, for example `01-lead-intake.v1.json`. Document webhook contracts, required credentials, error behavior, duplicate strategy, and verification in `IMPLEMENTATION.md`.
