# Repositories

Repository modules own SQLAlchemy queries and persistence operations. API endpoints and services should not scatter database queries throughout the codebase. Planned first module: `lead_repository.py`.
